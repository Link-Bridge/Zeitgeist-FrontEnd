export enum EnvKeysValues {
    BASE_API_URL = 'https://api.example.com',
}

export enum Routes {
    HOME = '/',
    PROJECTS = '/projects',
    TASKS = '/tasks',
    CLIENTS = '/clients',
    EMPLOYEES = '/employees',
}